{
  "name": "schema-finder",
  "private": true,
  "description": "Explore, modify, and create JSONSchema instances.",
  "homepage": "https://github.com/jamesbohacek/schema-finder#readme",
  "bugs": {
    "url": "https://github.com/jamesbohacek/schema-finder/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jamesbohacek/schema-finder.git"
  },
  "license": "Unlicense",
  "author": "jakub.bohacek <jamesbohacek@outlook.com>",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel -r dev",
    "format": "prettier --write 'packages/**/src/**/*.{ts,tsx,json}'",
    "format:verify": "prettier --list-different 'src/**/*.{ts,tsx,json}'"
  },
  "devDependencies": {
    "prettier": "^2.5.1"
  }
}
