{
  "name": "example",
  "version": "0.0.1",
  "description": "An example app to show and test the SchemaFinder component.",
  "main": "www/dist/index.js",
  "scripts": {
    "dev": "esbuild src/index.tsx --servedir=www --outdir=www/js --sourcemap --platform=browser --bundle",
    "build": "esbuild src/index.tsx --outdir=www/js --sourcemap --platform=browser --bundle"
  },
  "license": "Unlicense",
  "dependencies": {
    "react": "^16.8.0",
    "react-dom": "^16.8.0",
    "schema-finder": "workspace:*",
    "styled-components": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^16.8.0",
    "@types/react-dom": "^16.8.0",
    "esbuild": "^0.14.10",
    "typescript": "^4.5.4"
  }
}
